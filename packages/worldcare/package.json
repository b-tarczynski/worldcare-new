{
  "name": "worldcare",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ worldcare-new",
    "create-local": "graph create --node http://localhost:8020/ worldcare",
    "remove-local": "graph remove --node http://localhost:8020/ worldcare",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 worldcare",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.78.0",
    "@graphprotocol/graph-ts": "0.32.0",
    "get-graphql-schema": "^2.1.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
